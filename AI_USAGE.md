| Tool | Context / area | Problem / what was asked | Outcome |
| :---: | :---: | ----- | ----- |
| Claude Opus 4.8 | Project audit | Asked for a survey of a blank-slate frontend branch (empty Navbar/Footer stubs, Vite default `App.tsx`) and a clear view of what was owned. | Mapped the React 19 / Vite 8 / Tailwind v4 / Framer Motion / Router v7 stack, extracted the Figma palette, listed the owned routes and components per the roles, and proposed an order of work starting with the design system. |
| Claude Opus 4.8 | Tailwind v4 design system | Wanted the INTEGRA-TE colour tokens, fonts, and radii wired up as Tailwind utility classes. | Built the a skeleton token system in `index.css` using `@theme`: brand-blue/green/yellow/orange/red in 6 shades each, Nunito/Fredoka font tokens, `rounded-pill`/`rounded-card`/`rounded-chip` radii. Verified utilities were generated by the v4 compiler and manually checked. |
| Claude Opus 4.8 | Navbar | Needed a basic responsive navbar with desktop icon row, mobile burger menu, route highlights, ARIA, and accessibility. | Basic `Navbar.tsx` with Framer Motion drawer animation, focus management, escape-to-close, body-scroll lock, ARIA states. 56px mobile tap targets (above WCAG 44px floor). |
| Claude Opus 4.8 | HeroBrand component | Needed a boiler-platelanding-page hero (`<h1>` tagline \+ supporting paragraph \+ bg image) with responsive sizing and a subtle entrance animation. | `HeroBrand.tsx` with simple Framer Motion `slideUp` variants, and a basic responsive typography ramp to work with. |
| Claude Opus 4.8 | Content block renderers | The backend defines `Conteudo.tipo ∈ {texto, imagem, video, pdf}` and the frontend needed a dispatcher that rendered each correctly, including a GDPR-compliant YouTube embed. | Built `TextBlock`, `ImageBlock`, `VideoBlock` (privacy-safe facade: poster image until click, then `youtube-nocookie.com` iframe, no `autoplay`), `DownloadBlock` (gated by `descarregavel` \+ filename extraction), and the `ContentBlock` dispatcher. Modified manually to suit needs. |
| Claude Opus 4.8 | TopicDetail page (dynamic route) | The `/learn/:disciplinaSlug/:temaSlug` route was registered but the page file didn't exist. | Provided four render states (`loading`/`not_found`/`error`/`ok`), using `useTopicDetail` hook \+ `ContentBlock` dispatcher. `<main id="main-content">` for the skip-link target. Plus follow-up: fixed to `ImageBlock`'s empty `alt` fallback (`alt={block.titulo || 'Imagem ilustrativa'}`) and further refined to be optimised. |
| Whisper AI & OpenAI | Voice search | Consulted and used code from the official Whisper documentation to implement voice recognition and transcription. | Voice search functionality integrated into the platform. |
| Sentence Transformers & Hugging Face | Embeddings and semantic search | Consulted the Sentence Transformers documentation to understand how to turn text into embeddings and compare different content semantically. | Implementation of the vector representation of the content used in semantic search. |
| pgvector | Database and semantic search | Consulted the pgvector documentation and used AI support to understand how embeddings, vector storage, and similarity search work. | Storage and comparison of embeddings in the PostgreSQL database, enabling semantic search. |
| Claude Opus 4.8 | Accessibility with `Accessibility.tsx` | Asked for help understanding and implementing a functional modal with different accessibility settings, following the WCAG 2.1 AA guidelines applicable in Portugal. | Functional accessibility modal with several options aimed at improving usage of the platform. |
| Claude Opus 4.8 | Cards and database data fetching | Asked for an initial structure (boilerplate) for cards responsible for fetching and displaying information from the database. | The boilerplate was subsequently modified, adapted, and reused across the platform's various pages. |
| Claude Opus 4.8 | Styling and visual consistency | Once the first page had been built manually, asked the AI to replicate the same visual pattern on the remaining pages to maintain consistency and speed up development. | The manual design of `Aprender.tsx` was adapted to `Ler.tsx`, `Jogos.tsx`, `Video.tsx`, `Resolver.tsx`, and `Descarregar.tsx`. The style of `Privacidade.tsx` was adapted to `Rgpd.tsx`, `Privacy.tsx`, and `Faq.tsx`. The file `utils/dashboardTheme.ts` was also used to centralise theme elements. |
| Claude Opus 4.8 | Voice search API with `voice_search/views.py` | Asked for help connecting and organising the different components of the voice search API workflow. | Integrated flow between received audio, its transcription, search processing, and returning the results. |
| Claude Opus 4.8 | Database seeding with `seed_db.py` | Asked for help creating a script capable of inserting a large amount of data into the database using bulk creation operations. Some functions were already known, but used AI to speed up the process and generate an extensive and varied list of realistic objects. | Seed script using `bulk_create` that populates the database with a wider, more diverse set of test data. |
| Claude Opus 4.8 | Backend tests | Asked for an initial test scaffold for the backend. | Some of the generated tests were kept. Others were removed, modified, or replaced with tests better suited to the actual logic and needs of the application. |
| Github Copilot | `views.py` | Asked the AI to identify the imports required for the file and to help understand the overall structure of the code it should contain. | The required imports and a clearer picture of how the file should be organised, used as the starting point for the manual implementation. |
| Github Copilot | Serializer (`serializers.py`) | Asked the AI to explain how to write the serializer code. | Working understanding of how to write a DRF serializer, applied to the file. |
| Github Copilot | `Login.tsx` for cloud styling | Asked the AI to style the cloud element that sits in the middle of the page, behind the form used for entering the user's credentials. | Styled cloud background behind the login form, matching the rest of the page. |
| Github Copilot | `info.tsx` for hover hint  | Asked the AI to add a hint that appears when hovering over the button. | Hover hint implementation attempted on the button.  |
| Gemini 1.5 | Data Normalization & Adapters | Multiple data structures and contracts coming from different API endpoints (`aulasApi`, `exerciciosApi`, `videosApi`) needed to coexist seamlessly within the same carousel component. | Developed pure adapter functions (`aulaToVideo` and `exercicioToVideo`) that mapped and unified the varying backend properties into a single, stable data contract (`Video`). |
| Gemini 1.5 | Component Architecture & Pages | Needed to build a clean, consistent boilerplate and layout structure for the Sobre Page, FAQ Page and Quero Aprender from scratch. | Developed a strong structure, accessible layouts, and seamless state management for interactive accordion sections on the FAQ page.  |
| Gemini 1.5 | Core 4-Page Grid Systems | Needed to design and implement a completely synchronized 3x2 grid layout across the 4 core pages of the platform: *Quero Aprender*, *Quero Resolver*, *Quero Jogar*, and *Quero Ler*. | Engineered a unified responsive grid engine using Tailwind CSS (`lg:grid-cols-3 lg:grid-rows-2`), locking exactly 6 cards per view to keep a uniform spatial distribution across all primary routes. |
| Gemini 1.5 | UI Optimization (`BookCard.tsx`) | The main action buttons inside the dynamic cards suffered from vertical misalignment across the pages due to differing text title lengths and missing badges. | Solved the misalignment using CSS Flexbox properties, injecting `mt-auto` into the footers to pull and lock action triggers neatly to the bottom boundary of each card. |
| Chat GPT | Created various assets | Created some images to bring some colors to the website  | With the background removed already |