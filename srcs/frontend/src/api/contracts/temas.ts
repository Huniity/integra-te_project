export interface Tema {
  id: string;
  titulo: string;
  slug: string;
  seccao: string;
  disciplina: string;
  disciplina_nome: string;
}

export interface Disciplina {
  id: string;
  nome: string;
  slug: string;
}
